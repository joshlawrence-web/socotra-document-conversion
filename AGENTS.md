# Guided authoring has moved

The customer-facing template-authoring experience now lives in the sibling repo:

**https://github.com/joshlawrence-web/socotra-template-authoring**

(local clone expected next to this repo as `../socotra-template-authoring`)

## What to do

1. Open **`socotra-template-authoring`** as the agent workspace (not this repo).
2. Set `SOCOTRA_CONVERTER_ROOT` to **this** repo’s absolute path.
3. Follow that repo’s `AGENTS.md` / `template-authoring` skill.

## This repo

Keep using **[CLAUDE.md](CLAUDE.md)** for pipeline development, legs, tests, Leg 4,
live render, and `template-lite`. Do not use this file as a shortcut into guided
authoring — that would collapse the mechanical gap on purpose.
