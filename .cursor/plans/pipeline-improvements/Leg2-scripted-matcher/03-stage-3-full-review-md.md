# Stage 3 — Full review.md

**Status:** COMPLETE
**Depends on:** Stage 2 (generic matcher produces confidence + next_action per entry)
**Output:** `_write_review_md` in `leg2_fill_mapping.py` emits all 7 sections per `SKILL-output-formats.md`

---

## Overview

The current `_write_review_md` only writes a header + summary table. The full format (§1–§7) must be generated by the script so the AI skill doesn't need to re-read the suggested YAML and rewrite the file.

---

## Task 3.1 — Implement §3 Blockers

Every `low` entry (variables and loops). Render per `SKILL-output-formats.md` §3 format:

```markdown
### <placeholder_name>  _(line N)_

- **parent_tag:** `<tag>`
- **nearest_label:** "<label>"
- **loop:** `<loop name or —>`
- **candidates:** _(only when next-action is pick-one)_
  - `<path1>` — <why>
  - `<path2>` — <why>
- **next-action:** `<code>: <arg>`
- **suggested resolution:** <1–2 sentence prose>
```

The "suggested resolution" line is 1–2 sentences from a lookup table keyed by `next_action` code:

| next_action | Suggested resolution text |
|---|---|
| supply-from-plugin | No registry path exists for this field. A plugin (DocumentDataSnapshotPlugin or equivalent) must supply it as a scalar on `$data.data.*`. |
| restructure-template | A registry path exists but the template is missing the required `#foreach` wrapper. Add the foreach block shown in `requires_scope` and move this variable inside it. |
| pick-one | Multiple registry paths are equally plausible. Review the candidates and set `data_source` to the correct one before running Leg 3. |
| delete-from-template | This field has no business purpose in this document. Remove the placeholder. |
| confirm-assumption | (This code appears in §4 Assumptions, not §3 Blockers — medium only.) |

---

## Task 3.2 — Implement §4 Assumptions to confirm

Every `medium` + `confirm-assumption` entry. Render as GFM task list grouped by assumption text:

```markdown
## Assumptions to confirm

- [ ] **<assumption text>**
  - `$TBD_fieldName` (line N) → `$data.path`
```

The assumption text comes from the `reasoning` field — extract the "confirm-assumption: ..." portion.

---

## Task 3.3 — Implement §5 Cross-scope warnings

Entries where scope was violated (`violated_with_hint` or `violated_no_hint`). Table:

```markdown
## Cross-scope warnings

| Placeholder | Matched path | Requires scope | Fix |
|---|---|---|---|
| `$TBD_x` | `$vehicle.data.x` | `#foreach ($vehicle in $data.vehicles)` | restructure-template |
```

Always emit the heading; print "No cross-scope warnings." if empty.

---

## Task 3.4 — Implement §6 Done

High-confidence items in a collapsible `<details>` block:

```markdown
## Done

<details>
<summary><strong>N</strong> high-confidence mappings</summary>

- `field_name` → `$data.path`

</details>
```

---

## Task 3.5 — Implement §7 Unrecognised inputs

Feature flag rows + registry MINOR mismatch row. Four-column table per `SKILL-output-formats.md` §7.

For feature flags, derive the `Seen on` value as "all variables" (since flags apply to the whole registry, not per-entry).

---

## Task 3.6 — Wire terse vs full mode flags

The script runs in the mode passed via `--mode`. For `terse` mode:
- §3: emit only the table, no per-blocker prose
- §4: emit only "N assumptions to confirm — see .suggested.yaml"
- §6: omit the Done section prose, keep just the `<details>` block count

For `full` mode: emit all sections in full.

(The AI skill adds narrative depth on top in full mode — see Stage 4.)

---

## Acceptance criteria

- [x] `Simple-form.review.md` generated by script contains all 7 sections
- [x] §3 "No blockers" when all items are medium/high — emits "No blockers." line
- [x] §4 lists assumptions with correct placeholder names and paths
- [x] §5 "No cross-scope warnings." when none present
- [x] §6 shows high-confidence mappings in collapsible `<details>` block
- [x] §7 shows refusal flag rows for active feature flags
- [x] Terse mode produces condensed output (no per-blocker prose, single-line §4 summary)
- [x] Script output matches `SKILL-output-formats.md` spec; AI skill reads without rewriting

## Execution notes

All 7 sections implemented in `_write_review_md`. §3 in terse mode emits a compact table (placeholder name + line + next-action) rather than full per-blocker prose blocks. §4 in terse mode emits "N assumption(s) to confirm — see .suggested.yaml" rather than the full task-list. §6 always uses the `<details>` collapse block. The `_idx` dict (registry index built during `annotate_mapping`) is stashed on the `suggested` dict as `_idx` and stripped before YAML write; this lets `_write_review_md` access feature flags for §7 without re-parsing the registry.
