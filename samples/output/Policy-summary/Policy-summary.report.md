# Sanity report — Policy-summary.html

8 findings across 1 categories. Review before mapping; this skill never auto-fixes what it flags.

## Unlabeled variables

- **L43** — `{{manufacturerBrandCode}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
- **L44** — `{{goodsCategoryCode}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
- **L45** — `{{itemTypeCode}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
- **L46** — `{{purchaseDate}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
- **L47** — `{{purchasePrice}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
- **L48** — `{{levelOfCoverCategoryCode}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
- **L49** — `{{maxClaimAmount}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
- **L58** — `{{code}}` has no label immediately before it. Confirm the label is conveyed by layout, or add `<strong>...:</strong>` style tag.
