# Pipeline Test Suite

End-to-end integration tests for the five-leg document pipeline.
Covers the full path: DOCX → Leg 0 → conditional form → Leg 2+3 → Leg 4 (plugin).

## Quick start

```bash
# Step 1: generate the DOCX fixture files (one-time, or after changing fixtures)
python3 tools/generate_test_fixtures.py

# Step 2: run the full pipeline in automated mode
python3 tests/pipeline/run_test_pipeline.py --auto
```

Output lands in `tests/pipeline/output/<stem>/`.

---

## Modes

| Flag | Behaviour |
|------|-----------|
| *(none)* | Interactive — pauses after each Leg 0 run so you can hand-fill the conditional form |
| `--auto` | Automated — fills conditions from `condition_seeds.yaml` (CI-safe, no pauses) |
| `--only "TestItemCert(segment)"` | Run just one fixture (stem match, partial match works) |
| `--regen` | Regenerate DOCX fixtures before running |
| `--no-leg4` | Skip Leg 4 plugin generation |
| `--render-preview` | After Leg 2+3, render each `.final.vm` against a **live tenant** via the ad-hoc rendering endpoint (opt-in; see below) |

---

## Test fixtures

Stored in `tests/pipeline/fixtures/`. Generated by `tools/generate_test_fixtures.py`.

| File | Type | Fields | Conditionals |
|------|------|--------|--------------|
| `TestQuoteSummary(quote).docx` | quote | quoteNumber, account, startTime, endTime, jurisdiction, quotePremiumTotal, quoteOtherTotal | 2 |
| `TestItemCert(segment).docx` | segment | policyNumber, account, item (type/date/price/serial), coverage | 3 |
| `TestRenewalNotice(segment).docx` | segment | policyNumber, account, contractTermEndDate, expectedRenewalDate, discount, termChargesTotal | 3 |
| `TestItemsSchedule(segment).docx` | segment | account, `[Item/]` loop over items (type/date/price/serial) | 1 |
| `TestGiftSchedule(segment).docx` | segment | account, conditional `[Item/]` loop (type/price) | 2 |
| `TestCoverageGrid(segment).docx` | segment | account, `[Item/]` loop + `[Name?]` conditional row regions (in-loop coverage / doc-level presence / doc-level generic), coverage-hop fields | 4 |
| `TestCoverageSchedule(segment).docx` | segment | account, `[Coverage/]` plugin-list loop (itemTypeCode/displayName/labourCovered) | 1 |

Each document uses real registry accessor paths in `{field}` tokens so Leg 2 produces
high-confidence matches. Every conditional block is a named `[[$token]]` — a bare
`[[text]]` block is a hard error at Leg 0; the block's text lives only in the filled
`variants.csv`. `TestItemsSchedule` wraps its table's data row in `[Item/]`/`[/Item]`
marker rows — Leg 0 converts them to a `#foreach` scaffold and Leg 2+3 resolve it to
`#foreach ($item in $data.items)` so one row renders per item, left unconditional
(blank `when`). `TestGiftSchedule` fills the loop's own when-only `variants.csv` row
instead: the whole `#foreach` gets wrapped in `#if($data.Item)` and the plugin puts
the condition as a Boolean.

---

## Condition seeds

`condition_seeds.yaml` maps each document stem to a dict of `block_id → condition_expression`.
Conditions use valid accessor paths from `registry/path-registry.yaml`.

To add a new fixture:
1. Add a builder function in `tools/generate_test_fixtures.py` and append it to `FIXTURES`.
2. Add the fixture filename to `ALL_FIXTURES` in `run_test_pipeline.py`.
3. Add condition seeds for its blocks in `condition_seeds.yaml`.
4. Run `python3 tools/generate_test_fixtures.py` to write the new DOCX.

---

## What is tested

Each fixture run validates:
- **Leg 0** exits 0 and writes `.raw.html`, `.annotated.html`, `.mapping.yaml`, `.conditional-form.md`
- **Conditional form parse** exits 0 and writes `.conditional-registry.yaml`
- **Leg 2+3** exits 0 and writes `.final.vm`

The combined **Leg 4** run validates:
- All successful mappings can be combined into a single `ZenCoverDocumentDataSnapshotPluginImpl.java`
- Additive mode works when more than one mapping is processed

---

## Render preview (opt-in, live tenant)

`--render-preview` POSTs each fixture's `.final.vm` to Socotra's ad-hoc rendering
endpoint (`POST {API_URL}document/{tenantLocator}/documents/render`, Documents API)
so the template renders against real tenant data without conducting any transaction.

Everything travels as **multipart form-data**: `referenceType`, `referenceLocator`,
`templateFormat=velocity`, `productName`, the template source itself (`template`),
and an inline `documentConfig` JSON (a `DocumentConfigRef` — name/scope/format/
rendering/trigger/portrait/pageSize). The inline JSON is self-sufficient: the named
document config does **not** need to be deployed on the tenant.

Prerequisites:
1. **Deploy the generated `DocumentDataSnapshotPlugin` to the tenant first.** The
   renderer executes it to build `$data` — conditionals included. Without it,
   `$data.condN` references fail under Velocity strict rendering.
   (Deploy automation is planned; for now deploy manually.)
2. Copy `.env.ai-documents.example` → `.env.ai-documents` (repo root, gitignored)
   and fill in `AI_DOCUMENTS_API_URL`, `AI_DOCUMENTS_TENANT_LOCATOR`,
   `AI_DOCUMENTS_PAT` (JWT or PAT with the `documents` group's
   `render-external` permission), and a live locator per reference type
   (`AI_DOCUMENTS_REFERENCE_QUOTE`, `AI_DOCUMENTS_REFERENCE_SEGMENT`, ...).
   Process env vars with the same names override the file.
3. `python3 tests/pipeline/run_test_pipeline.py --auto --render-preview`

Each fixture's reference type comes from its stem — `TestQuoteSummary(quote)`
renders against `AI_DOCUMENTS_REFERENCE_QUOTE`, `TestItemCert(segment)` against
`AI_DOCUMENTS_REFERENCE_SEGMENT`. Fixtures whose type is unset are SKIPped.
A render passes if the endpoint returns 2xx with a non-empty body; text output
must also contain no leftover `$TBD_` / `$doc.cond` tokens (PDF bytes can't be
grepped). Output: `tests/pipeline/output/<stem>/<stem>.preview.pdf` (or `.html`).

One-off previews outside the suite:

```bash
python3 -m velocity_converter.render_preview \
  --template workspace/output/<stem>/<stem>.final.vm \
  --reference-type quote --reference-locator <locator> \
  --out workspace/output/<stem>/<stem>.preview.pdf
```

Use `--document-config <path.json>` to send a custom inline `DocumentConfigRef`
instead of the built-in pdf/dynamic default.

---

## Running in CI

```bash
# CI command (no interactive pauses, non-zero exit on failure)
python3 tests/pipeline/run_test_pipeline.py --auto
```

Add `--regen` if fixtures are not committed to the repo.

---

## Troubleshooting

**"fixture file(s) missing"** → Run `python3 tools/generate_test_fixtures.py` first.

**"Leg 0 failed"** → Check that `python-docx` is installed: `pip install python-docx --break-system-packages`

**"Leg 2+3 failed"** → Check `tests/pipeline/output/<stem>/<stem>.review.md` for unresolved tokens.

**Conditions not matching** → Update `condition_seeds.yaml` with valid accessor paths.
Run `python3 -m velocity_converter.list_paths` to see the full registry.
